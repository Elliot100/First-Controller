<h1>All the users!</h1>

<%# ruby code that will not be printed%>
<%#= ruby code that we want to render%> 


<ul>
    <% @users.each do |user| %>
        <li>
        <a href="<%= user_url(user) %>"> <%= user.name %></a>
        </li>
    <% end %>
</ul>